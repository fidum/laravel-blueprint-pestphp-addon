models:
  Certificate:
    name: string
    certificate_type_id: id:certificate_type
    reference: string
    document: string
    expiry_date: date
    remarks: text nullable
  CertificateType:
    name: string

controllers:
  Certificate:
    resource: api
